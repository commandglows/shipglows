---
name: sg-engineering
description: Architecture, quality, dependencies, performance, migrations, sync, access, and parity.
---

# sg-engineering

## Mission

`sg-engineering` is the public owner for architecture, quality, dependencies, performance, migrations, sync, access, and parity.

## Scope Gate

Resolve the product and technical surface, load `$SHIPGLOWS_ROOT/skills/references/intent-to-outcome-autonomy.md`, and make safe technical decisions from repository evidence. Keep specialist sync, access, and parity engines internal.

## Required References

Resolve `$SHIPGLOWS_ROOT` through the shared canonical-path doctrine, verify the root and `$SHIPGLOWS_ROOT/skills/010-sg-technical/SKILL.md` exist, then load that canonical engine for the detailed engineering workflow and proof gates. If the root or file is missing, stop with a visible error; never fall back to a sibling runtime path. Continue it autonomously; `010-sg-technical` is expert/legacy only.

Hidden expert modes are `verify`, `test`, and `browser`. They select
`103-sg-verify`, `107-sg-test`, and `108-sg-browser` respectively while this
skill retains technical-quality ownership. Their `shipglows` aliases are
convenience routes only. When `verify` names an explicit design, SEO, release,
or bug scope, the router preserves that specialist public owner instead.

## Validation

Use the selected workflow's technical checks and proportional proof; do not report an engineering outcome from an unverified code change alone.

## Stop Conditions

Stop and follow the runtime contract when technical authority, risk, scope, required evidence, or a material product decision is unresolved.

## Report Modes

Load `$SHIPGLOWS_ROOT/skills/references/reporting-contract.md` before reporting; keep internal engine names out of user-facing reports.
