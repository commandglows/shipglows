interface:
  display_name: "ShipGlows Private Memory"
  short_description: "Remember private pointers and access declared data"
  default_prompt: "Use $sg-private to remember a private pointer or access an explicitly declared private-data namespace."
policy:
  allow_implicit_invocation: true
