---
name: sg-design
description: Design interfaces, systems, accessibility, audits, inspiration, and animation.
---

# sg-design

## Mission

`sg-design` is the public owner for interfaces, design systems, accessibility, inspiration, and animation.

## Scope Gate

Resolve the affected product and surface, then load `$SHIPGLOWS_ROOT/skills/references/intent-to-outcome-autonomy.md`. Retain outcome ownership and clarify only decisions that materially affect the result.

## Required References

Resolve `$SHIPGLOWS_ROOT` through the shared canonical-path doctrine, verify the root and `$SHIPGLOWS_ROOT/skills/006-sg-design/SKILL.md` exist, then load that canonical engine for design, animation, accessibility, and proof playbooks. If the root or file is missing, stop with a visible error; never fall back to a sibling runtime path. Execute it end to end; `006-sg-design` remains an expert/legacy engine only.

## Validation

Use the selected runtime playbook's proof path; do not replace visible, accessibility, or token proof with a code-only claim.

## Stop Conditions

Stop and route through the runtime contract when design intent, authority, required proof, or scope is unresolved.

## Report Modes

Load `$SHIPGLOWS_ROOT/skills/references/reporting-contract.md` before reporting; keep internal engine names out of user-facing reports.
