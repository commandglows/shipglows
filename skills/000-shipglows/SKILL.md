---
name: 000-shipglows
description: "Execute clear bounded requests directly and route unknown, unbounded, or directional work."
argument-hint: <instruction>
---

Primary artifact type: `entrypoint-router`.

## Canonical Paths

Before resolving owned files, load `$SHIPGLOWS_ROOT/skills/references/canonical-paths.md`.
All shared references below resolve under `$SHIPGLOWS_ROOT/skills/references/`;
never substitute project shadows or memory.

## Intent-to-Outcome Ownership

For non-trivial routing load `intent-to-outcome-autonomy.md`; resolve
`project -> business/brand/product -> outcome -> surface -> work item` from evidence.
Continue under one public owner in this conversation. When sufficiency, authority,
freshness, conflict, compaction or handoff can change the route, load
`context-quality-contract.md`; preserve its qualified Context Capsule, never memory as truth.

## Chantier Tracking

Trace category: `non-applicable`. Process role: `helper`.
The owner writes lifecycle state; this router never writes specs, bug/release scopes,
Skill Run History or deployments. Use `(local)` when needed. Authorized bounded
execution below is the exception, not permission for a chantier.

## Report Modes

Load `reporting-contract.md` before final reporting. Default `report=user`, concise
and in the active language; blocked routing stays user mode. Agent detail requires
explicit request. Continue safely runnable authorized work instead of returning a command.

## Explicit Invocation Preflight

Explicit invocations, including `$shipglows`, load `skill-invocation-preflight.md`
and run its checker: valid stays silent; invalid/ambiguous activates no substitute.
First-token expert aliases load `expert-mode-aliases.md` before natural-language
routing; preserve public owner -> owner mode -> internal engine. Numeric legacy
commands resolve through `skill-code-index.md`. Aliases are never shell arguments.

## Bounded Direct-Execution Gate

Before loading routing, topology, or owner-skill references, keep clear bounded
requests in direct main-thread execution: few enumerable actions/targets, discoverable
by focused lookup, no material direction to select, and focused proof sufficient.
This includes exact edits, exact-scope commits, ordinary resolved pushes or a small
sequence; local/remote and model reasoning do not change classification. It does not authorize a chantier.

Before any direct or routed mutation, load `mutation-plan-approval.md`. Apply its
request-derived or standing authority; otherwise use 🧭 VALIDATION RAPIDE for a
bounded agent proposal and 🧭 PLAN À VALIDER for unbounded/directional work.
Material expansion pauses. Preserve unrelated dirty work and all dedicated safety gates.

Do not load a domain or lifecycle skill: direct work needs no owner skill.
An explicitly named skill still activates in its smallest safe mode;
let that skill reroute explicitly.
`skill-execution-fidelity.md` is advisory here; apply the gate above and smallest relevant proof.

### Bounded internal reference-register updates

A supplied-link update qualifies when one focused local lookup suffices, public
URLs/category are supplied, `candidate` is the only added status, no interpretation
or public claim is needed, and duplicate/row proof suffices. Use original authority,
append factually without launching source intake, market study, or documentation topology work;
otherwise normal routing/approval applies. Use one primary-source check per supplied reference.
`veille <URL>` analyzes without persistence; `concurrent <URL>` or `inspiration <URL>`
updates its register. `veille` takes precedence.

## Delegation And Topology

Direct handoff needs no delegation procedure. Never launch the selected master as
a nested subagent; the owner controls topology. Load `master-delegation-semantics.md`
before actual delegation. A routing scout must pass that gate, stays read-only,
cannot invoke skills, mutate or delegate, and returns only a route recommendation.

## Shared Routing Reference

Load `decision-quality-contract.md` before non-trivial outcome selection.
Known ShipGlows skill/doctrine audit -> `900-shipglows-core audit`; known skill
change -> `900-shipglows-core build`. Only unresolved owner/mode loads
`entrypoint-routing.md`. Do not load the matrix after a route is resolved.
Pure help needs no outcome execution, matrix, delegation or procedural pack.
Named profiles load `profile-activation.md` and minimal profile context; focus tags
load `shipglows-terms.md` and matching explicit gates; source intake loads
`source-intake-classification.md`. Sparse/partnership decisions load
`operator-partnership-contract.md` through the intention core.

URL/browser/MCP/app/connector/runtime-dependent work loads `agent-runtime-awareness.md`:
read the global environment, managed project ENVIRONMENT and matching live registry.
Current-turn direct/deferred tools determine callability, not recorded installation.
Never start a replacement server or guess a default URL.

## Mission

ShipGlows maintenance targets `$SHIPGLOWS_ROOT`, not the current application unless explicitly
specified otherwise. `core` binds every remaining word to the system, including
quoted application evidence; never redirect part of it to that application.

## Mode Detection

Parse `$ARGUMENTS`. Empty/help answers directly; full help may use `302-sg-help`.
An explicitly named skill remains authoritative. Natural language applies the
bounded gate first, then selection. Explicit maintainer workstation, editable full
clone or "version dev de ShipGlows" preserves installation ownership; generic
`full`, `all`, environment or corpus never implies a maintainer channel switch.

Before selection extract `#local`, `#nolocal`, `#ci` wherever present and load
`execution-posture-tags.md`; reject conflicts. Preserve ordinary focus tags and
owner authority; `#ci` implies nolocal and deferred proof, never external-write authority.

For `update`, `context`/`contexte`/`env`/`environment`, `auto`, or legacy `nolocal`, load
`$SHIPGLOWS_ROOT/skills/references/entrypoint-special-modes.md`. Apply only the selected mode.
Context is read-only; update resolves channel first. Explicit Auto implies nolocal,
freezes the current project, rejects local, and never means `300-sg-docs auto`.
Legacy nolocal requires an objective, loads `no-local-execution-policy.md`, normalizes
to `<objective> #nolocal`, and preserves ordinary ownership/approval. Bare nolocal
never invokes Auto or selects work.

`git` -> `sg-engineering github` (read-only default); `hygiene` -> current-project
`sg-maintenance hygiene` read-only audit; `hygiene git` -> `sg-engineering github clean`.
Reject workspace/multi-project hygiene. `verify` preserves design/accessibility/UI,
SEO, release/live or bug specialist ownership, otherwise `sg-engineering verify`.
Generic verification never replaces a specialist audit.
Known helpers: context priming -> `301-sg-context`; cross-project state ->
`308-sg-status`; paused work -> `706-continue`. Alias tables own remaining mappings.

## Handoff And Question Contract

Pass original instruction, arguments, scope, authority, active language and explicit
report mode to one owner and continue. Preserve onboarding/first-success intent
through `sg-development`. Before routing questions load `question-contract.md`;
ask one numbered question only for material operator-owned truth changing route or
safety, never discoverable paths. Never stop at a command recommendation for runnable work.

## Stop Conditions

Stop for missing contracts, unresolved material product/data/security/permission/
deployment/ship decisions, nested master execution, unapproved destructive/production/
payment/auth/tenant/secret/dirty-file scope, or bypassed evidence, verification,
closure or ship gates. Material expansion never inherits silent authorization.

## Final Report

Apply the report contract; show routing only when useful. A valid route is not completion proof.
