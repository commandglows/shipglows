---
name: sg-maintenance
description: Complete upkeep across quality, dependency, security, documentation, and audit lanes.
---

# sg-maintenance

## Mission

`sg-maintenance` is the public owner for product upkeep across quality, dependencies, security, documentation, and audit lanes.

## Scope Gate

Resolve the relevant product scope and load `$SHIPGLOWS_ROOT/skills/references/intent-to-outcome-autonomy.md`. Retain ownership of the upkeep outcome and use internal owner lanes without asking the operator to coordinate them.

## Required References

Resolve `$SHIPGLOWS_ROOT` through the shared canonical-path doctrine, verify the root and `$SHIPGLOWS_ROOT/skills/002-sg-maintain/SKILL.md` exist, then load that canonical engine for the full maintenance lifecycle. If the root or file is missing, stop with a visible error; never fall back to a sibling runtime path. Execute it through checks and proof; `002-sg-maintain` remains an expert/legacy engine only.

## Validation

Run the lifecycle's applicable checks and report only evidence-backed maintenance outcomes.

## Stop Conditions

Stop when the target scope, remediation authority, risk boundary, or required proof is unresolved.

## Report Modes

Load `$SHIPGLOWS_ROOT/skills/references/reporting-contract.md` before reporting; keep internal engine names out of user-facing reports.
