---
artifact: bug_record
metadata_schema_version: "1.0"
artifact_version: "1.0.0"
project: "ShipGlows"
created: "2026-08-23"
updated: "2026-08-23"
status: active
source_skill: "sg-fix"
scope: "windows-flutter-web-browser-wiring"
owner: "Diane"
bug_id: "BUG-2026-08-23-003"
title: "Flutter Web ignores the Chromium already managed by ShipGlows"
bug_status: "fixed-pending-verify"
severity: "high"
reported_by: "operator"
first_observed: "2026-08-23"
last_observed: "2026-08-23"
environment: "Windows 11; PowerShell; managed Playwright Chromium"
reproducibility: "always"
redaction_status: "not-required"
related_bugs: ["BUG-2026-08-23-001", "BUG-2026-08-23-002"]
related_artifacts: ["cli/windows/install-devserver.ps1", "cli/windows/ShipGlows.FlutterSupervisor.ps1"]
confidence: high
risk_level: high
security_impact: none
docs_impact: yes
evidence:
  - "The installer validated a full Playwright Chromium executable but did not expose its path to Flutter."
  - "Flutter doctor reported Chrome missing and the DevServer still defaulted Flutter Web to the chrome device."
  - "The first live fix attempt exposed no path because chrome.exe --version opens the Windows GUI browser and times out instead of producing console version evidence."
depends_on: []
supersedes: []
next_step: "Rerun the corrected branch installer and verify Flutter doctor plus the managed browser environment."
---

# Bug Record: BUG-2026-08-23-003

## Summary

ShipGlows already installed and validated Chromium for its managed Playwright runtime, but Flutter Web had no `CHROME_EXECUTABLE` mapping and therefore considered the Chrome toolchain unavailable.

## Expected Behavior

- A runnable managed Playwright Chromium path is returned by the installer runtime probe.
- The full installer persists that exact path in `CHROME_EXECUTABLE` for the user and current process.
- The Flutter supervisor forwards only an existing executable under the managed Playwright root and rejects reparse paths.
- Explicit `web-server` mode remains independent of a browser executable.

## Fix and Proof

The installer now exposes the validated full browser path and the supervisor revalidates it before launching Flutter's `chrome` device. Playwright's matching headless-shell executable provides bounded version evidence because the full Windows GUI executable does not implement a terminating console `--version` probe. Static contracts and focused supervisor path tests cover managed acceptance, external-path rejection, and browser-free `web-server` mode. Live doctor and preview verification remain pending.

## Next Step

Rerun the corrected branch installer, verify `CHROME_EXECUTABLE` and `flutter doctor -v`, then exercise a managed Flutter Web start in a separate approved project mutation scope if needed.
