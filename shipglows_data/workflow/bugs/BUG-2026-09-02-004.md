---
artifact: bug_record
metadata_schema_version: "1.0"
artifact_version: "0.1.0"
project: "ShipGlows"
created: "2026-09-02"
updated: "2026-09-02"
status: active
source_skill: "sg-fix"
scope: "windows-runtime-update-packaging"
owner: "Diane"
bug_id: "BUG-2026-09-02-004"
title: "Windows runtime transaction requires launchers before they are generated"
bug_status: "in-diagnosis"
severity: "high"
reported_by: "operator"
first_observed: "2026-09-02"
last_observed: "2026-09-02"
environment: "Windows 11, linked maintainer channel, s update"
reproducibility: "always"
redaction_status: "not-required"
related_bugs: []
related_artifacts:
  - "install-shipglows.ps1"
  - "cli/windows/install-devserver.ps1"
  - "tests/windows/runtime-update-transaction.ps1"
confidence: high
risk_level: high
security_impact: no
docs_impact: yes
evidence:
  - "s update stops before mutation with: Staged runtime file is missing: bin/s.exe"
  - "The transaction prevalidates every managed path before install-devserver.ps1 can compile the native launchers."
depends_on: []
supersedes: []
next_step: "/sg-fix BUG-2026-09-02-004"
---

# Bug Record: BUG-2026-09-02-004

## Summary

- The Windows full-runtime updater declares three native launchers as managed payload files even though the installer generates them inside the transaction action.
- The transaction therefore rejects a valid staged payload before the launcher compiler can run.

## Reproduction

- Preconditions:
  - Use the linked ShipGlows maintainer channel on Windows.
  - Start from a clean checkout whose full payload contains launcher source but no precompiled launchers.
- Steps:
  1. Run `s update`.
  2. Let the bootstrap stage the full Windows runtime.
  3. Observe transaction prevalidation before the installer action.

## Expected Behavior

- Staged files are activated atomically, the installer generates the native launchers, generated targets are verified, and the managed manifest is committed only after success.

## Observed Behavior

- The update stops before mutation because `bin/s.exe` is listed as managed but absent from the staged payload.

## Evidence

- Reproduction output: `Staged runtime file is missing: bin/s.exe`.
- Source inspection: launcher compilation occurs in `install-devserver.ps1`, which is invoked only inside the transaction action.

## Diagnosis Notes

| Date UTC | Skill | Author | Hypothesis | Findings | Outcome |
|----------|-------|--------|------------|----------|---------|
| 2026-09-02 16:52:54 UTC | sg-fix | Codex | Generated launchers need a distinct transaction lifecycle from staged payload files. | Prevalidation and copy treat all managed paths as staged; the action that creates launchers is unreachable. | supported |

## Fix Attempts

| Date UTC | Skill | Author | Changes | Validation | Result | Next Step |
|----------|-------|--------|---------|------------|--------|-----------|

## Retest History

| Date UTC | Skill | Retest Command | Environment | Result | Notes |
|----------|-------|----------------|-------------|--------|-------|

## Redaction Status

- Current redaction status: `not-required`
- Redaction notes: no secrets, credentials, private payloads, or personal data are involved.

## Closure Criteria

- A generated managed path may be absent from the payload and must exist after the action.
- Missing generated output or an action exception restores the runtime byte-for-byte.
- Ordinary staged paths still fail before mutation when absent.
- Repeated successful activation remains byte-idempotent and concurrent activation remains locked.
- The real `s update` path no longer fails on a missing staged launcher.

## Next Step

Implement and retest the action-generated managed-path contract.
